#!/usr/bin/env node

const app = require('../app');
const server = require('../server');

/**
 * Listen on provided port, on all network interfaces.
 * Get app and server  - and start listeing to a port
 */

server.listen(app.get('port'));
